{
  "name": "@repo/subgraph",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.93.2"
  }
}
